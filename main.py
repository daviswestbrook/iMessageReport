import Message

# Get messages

messages = Message.get_message_stream()

# Find groups
groups = Message.get_groups()

# Ask user to specify group
group_ID = Message.specify_groups()

# Get handles in the group

# Clean up texters with user help

# Make report

